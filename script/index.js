var app = angular.module('myapp', []);
app.controller('myController', function($scope, $http) {
    $scope.maorong = product.maorong;
    $scope.huwai = product.huwai;
    $scope.jimu = product.jimu;
    $scope.diandong = product.diandong;
});
